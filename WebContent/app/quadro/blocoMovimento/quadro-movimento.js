angular.module('appControllers')
  .controller('QuadroLarvaCtrl', function ($scope, $location, ContaService) {

    /*
    document.getElementById('ball2').onmousedown = function() {
      //alert('HAAAAAAAA');
      this.style.position = 'absolute'

      var self = this

      document.onmousemove = function(e) {
        e = e || event
        //fixPageXY(e)

        self.style.left = e.pageX-25+'px'
        self.style.top = e.pageY-25+'px'
      }
      this.onmouseup = function() {
        document.onmousemove = null
      }
    }

    document.getElementById('ball2').ondragstart = function() { return false }

    document.onmousemove = function(e) {
      e = e || window.event
      //fixPageXY(e)
      var x = document.getElementById('mouseX').value = e.pageX
      var y = document.getElementById('mouseY').value = e.pageY
      console.log(x +', '+ y);
    }

  	var params = {
        start: function(event, ui) {
            console.log("Rotating started");
        },
        rotate: function(event, ui) {
            console.log("Rotating");
        },
        stop: function(event, ui) {
            console.log("Rotating stopped");
        },
    };

  	$('#target3').resizable().rotatable(params);
  	$( "#draggable" ).draggable();
  	$('#id').innerHTML = 'haha';

  	//document.getElementById('dragable3').draggable();
    */

});
