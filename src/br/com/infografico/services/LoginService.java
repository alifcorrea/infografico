package br.com.infografico.services;


public class LoginService {
    
/*    @Inject
    private UsuarioRepository usuarioRepo;
    
    public Sessao login(String email, String senha, String ip) {
        
        Sessoes sessoes = Sessoes.getInstance();
        
        Usuario usuario = usuarioRepo.findByEmail(email);
        if (usuario.getSenha().equals(senha)) {
            Sessao sessao = new Sessao(usuario, ip);
            sessoes.getHashSessoes().put(sessao.getSessionId(), sessao);
            return sessao;
        } else {
            throw new MORuntimeException("Usuário ou Senha inválidos");
        }
    }*/
}
